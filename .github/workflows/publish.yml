name: Publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  build-napi:
    name: Build NAPI - ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04
            target: aarch64-unknown-linux-gnu
          - os: windows-2025
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1.10.0
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: ${{ matrix.target }}

      - name: Install zig (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        uses: goto-bus-stop/setup-zig@abea47f85e598557f500fa1fd2ab7464fcb39406 # v2.2.1
        with:
          version: 0.13.0

      - name: Install cargo-zigbuild (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: cargo install cargo-zigbuild

      - name: Build NAPI
        working-directory: crates/ox_content_napi
        run: vp exec -- napi build --release --target ${{ matrix.target }} ${{ matrix.target == 'aarch64-unknown-linux-gnu' && '--cross-compile' || '' }}

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: napi-${{ matrix.target }}
          path: crates/ox_content_napi/*.node
          if-no-files-found: error

  publish-napi:
    name: Publish @ox-content/napi
    runs-on: ubuntu-24.04
    needs: build-napi
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1.10.0
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: true

      - name: Setup npm registry
        run: npm config set registry https://registry.npmjs.org

      - name: Install npm 11+ (required for OIDC trusted publishing)
        run: npm install -g npm@11

      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: napi-*
          path: crates/ox_content_napi/artifacts

      - name: Create npm sub-package directories
        working-directory: crates/ox_content_napi
        run: vp exec -- napi create-npm-dirs --npm-dir ../../binding-packages

      - name: Move artifacts to sub-packages
        working-directory: crates/ox_content_napi
        run: |
          declare -A target_map=(
            ["x86_64-apple-darwin"]="darwin-x64"
            ["aarch64-apple-darwin"]="darwin-arm64"
            ["x86_64-unknown-linux-gnu"]="linux-x64-gnu"
            ["aarch64-unknown-linux-gnu"]="linux-arm64-gnu"
            ["x86_64-pc-windows-msvc"]="win32-x64-msvc"
          )
          for dir in artifacts/napi-*; do
            target="${dir##*/napi-}"
            platform="${target_map[$target]}"
            if [ -n "$platform" ]; then
              for f in "$dir"/*.node; do
                cp "$f" "../../binding-packages/${platform}/ox-content.${platform}.node"
              done
            fi
          done
          echo "=== Verify binding packages ==="
          for pkg in ../../binding-packages/*/; do
            ls -la "$pkg"*.node 2>/dev/null || echo "WARNING: No .node file in $pkg"
          done

      - name: Determine npm tag
        id: npm-tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [[ "$VERSION" == *"-"* ]]; then
            PRE_ID="${VERSION#*-}"
            TAG="${PRE_ID%%.*}"
          else
            TAG="latest"
          fi

          if [[ ! "$TAG" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*$ ]]; then
            echo "::error::Invalid npm dist-tag: $TAG"
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "NPM_TAG=$TAG" >> "$GITHUB_ENV"

      - name: Update optionalDependencies
        working-directory: crates/ox_content_napi
        run: vp exec -- napi pre-publish -t npm --npm-dir ../../binding-packages --no-gh-release --skip-optional-publish

      - name: Publish binding sub-packages
        run: |
          publish_if_needed() {
            local pkg_name pkg_version
            pkg_name=$(node -p "require('./package.json').name")
            pkg_version=$(node -p "require('./package.json').version")

            if npm view "${pkg_name}@${pkg_version}" version >/dev/null 2>&1; then
              echo "Skipping ${pkg_name}@${pkg_version} (already published)"
              return 0
            fi

            npm publish --provenance --access public --tag "$NPM_TAG"
          }

          for pkg in binding-packages/*/; do
            if [ -f "$pkg/package.json" ]; then
              echo "Publishing $(basename $pkg)..."
              (cd "$pkg" && publish_if_needed)
            fi
          done

      - name: Publish @ox-content/napi
        working-directory: crates/ox_content_napi
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          npm publish --provenance --access public --tag "$NPM_TAG"

  publish-packages:
    name: Publish npm packages
    runs-on: ubuntu-24.04
    needs: publish-napi
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1.10.0
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: true

      - name: Setup npm registry
        run: npm config set registry https://registry.npmjs.org

      - name: Install npm 11+ (required for OIDC trusted publishing)
        run: npm install -g npm@11

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: wasm32-unknown-unknown

      - name: Build NAPI (for local use)
        run: vp run build:napi

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - name: Build packages
        run: vp run build:npm

      - name: Build WASM package
        run: vp run build:wasm

      - name: Determine npm tag
        id: npm-tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [[ "$VERSION" == *"-"* ]]; then
            PRE_ID="${VERSION#*-}"
            TAG="${PRE_ID%%.*}"
          else
            TAG="latest"
          fi

          if [[ ! "$TAG" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*$ ]]; then
            echo "::error::Invalid npm dist-tag: $TAG"
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "NPM_TAG=$TAG" >> "$GITHUB_ENV"

      - name: Publish @ox-content/islands
        working-directory: npm/ox-content-islands
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/vite-plugin
        working-directory: npm/vite-plugin-ox-content
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/unplugin
        working-directory: npm/unplugin-ox-content
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/vite-plugin-vue
        working-directory: npm/vite-plugin-ox-content-vue
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/vite-plugin-react
        working-directory: npm/vite-plugin-ox-content-react
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/vite-plugin-svelte
        working-directory: npm/vite-plugin-ox-content-svelte
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          TARBALL=$(vp exec -- pnpm pack --pack-destination /tmp | tail -1)
          npm publish "$TARBALL" --provenance --access public --tag "$NPM_TAG"

      - name: Publish @ox-content/wasm
        working-directory: crates/ox_content_wasm/pkg
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "Skipping ${PKG_NAME}@${PKG_VERSION} (already published)"
            exit 0
          fi
          npm publish --provenance --access public --tag "$NPM_TAG"

  publish-crates:
    name: Publish to crates.io
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable

      - name: Authenticate with crates.io
        id: crates-io-auth
        uses: rust-lang/crates-io-auth-action@bbd81622f20ce9e2dd9622e3218b975523e45bbe # v1.0.4

      - name: Publish crates (in dependency order)
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token }}
        run: |
          # Publish in dependency order with delays for crates.io index update
          cargo publish -p ox_content_allocator --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_ast --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_parser --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_renderer --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_og_image --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_search --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_ssg --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_docs --allow-dirty || true
          sleep 30
          cargo publish -p ox_content_vite --allow-dirty || true

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-24.04
    needs: [publish-packages, publish-crates]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REF_NAME: ${{ github.ref_name }}
        run: gh release create "$GH_REF_NAME" --generate-notes
