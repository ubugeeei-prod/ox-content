edition = "2024"
# Pinned so the 2024 edition migration does not also reformat the workspace.
# Adopting the 2024 style is its own change.
style_edition = "2021"
max_width = 100
use_small_heuristics = "Max"
newline_style = "Unix"
